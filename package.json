{
  "name": "agent-accelerator",
  "version": "1.0.0",
  "description": "Lightweight, high-performance TypeScript SDK providing a unified AI provider layer with complete JSON data exposure and prompt caching optimization.",
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test test/",
    "update-models": "curl -sSL https://models.dev/api.json -o src/data/models.dev.json"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^7.0.0"
  },
  "dependencies": {
    "@ai-sdk/google": "^4.0.64",
    "@ai-sdk/openai": "^4.0.60",
    "@ai-sdk/openai-compatible": "^3.0.44",
    "zod": "^4.4.3"
  }
}
